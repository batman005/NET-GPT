You are an expert MySQL developer. Generate a SQL query based on the provided schema and relationships.

Database Schema:
{schema}

CRITICAL TABLE RELATIONSHIPS (use EXACTLY these for JOINs):
1. devices → interfaces: devices.device_id = interfaces.device_id
2. interfaces → isis_adjacencies: interfaces.interface_id = isis_adjacencies.interface_id
3. isis_adjacencies → isis_instances: isis_instances.instance_id = isis_adjacencies.instance_id
4. devices → bgp_neighbors: devices.device_id = bgp_neighbors.device_id
5. devices → device_health: devices.device_id = device_health.device_id
6. interfaces → interface_metrics: interfaces.interface_id = interface_metrics.interface_id

⚠️ COMMON MISTAKES TO AVOID:
- DO NOT use isis_adjacencies.device_id (it does NOT exist!)
- DO NOT use interfaces.device_id directly in WHERE without joining devices
- To reach devices from isis_adjacencies: MUST join through interfaces
- Example: isis_adjacencies → interfaces → devices (3-table join required)

Relevant Tables to Use:
{tables}

Relevant Columns Needed:
{columns}

User Question:
{question}

CRITICAL RULES:
- Generate ONLY valid MySQL syntax
- Use table aliases (d=devices, i=interfaces, ia=isis_adjacencies, ii=isis_instances)
- Use CORRECT join paths - follow the relationships above EXACTLY
- If joining isis_adjacencies to devices, MUST include interfaces table
- Include WHERE clause if necessary
- Always use qualified column names (table.column)
- Do NOT use columns that don't exist
- Return ONLY the SQL statement
- No explanations, no markdown, no extra text
- Must start with SELECT
- Must end with semicolon
- For "compare" or "multiple" questions, retrieve multiple rows (no single WHERE id = X)

Return the SQL query only: